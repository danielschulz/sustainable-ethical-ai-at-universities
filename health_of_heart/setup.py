from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Analyze risk factors of strokes in order to prevent and predict fatal instances.',
    author='Daniel Schulz',
    license='MIT',
)
